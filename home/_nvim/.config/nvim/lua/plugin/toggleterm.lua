require("toggleterm").setup({
    direction = 'horizontal',
    close_on_exit = false,
    auto_scroll = true,
    start_in_insert = true,
    insert_mappings = true,
    open_mapping = [[<C-\>]],
})
