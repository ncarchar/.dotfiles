#!/usr/bin/env zsh

selected=$(fc -ln -t '%F %T' | fzf --border --ansi --height 40% | sed 's/[^ ]* [^ ]* //')

if [[ -n $selected ]]; then
    echo -n "$selected" | xclip -selection clipboard
    echo "'$selected' copied to clipboard."
fi
